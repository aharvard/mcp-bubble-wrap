services:
  - type: web
    name: mcp-bubble-wrap
    runtime: node
    region: virginia
    plan: free
    buildCommand: pnpm install && pnpm build
    startCommand: pnpm start
    envVars:
      - key: NODE_ENV
        value: production

